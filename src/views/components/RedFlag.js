let RedFlag = {
  render: async (post) => {
    return /*html*/ `
            <div class="red-flag card">
                
            </div>
        `;
  },
  // All the code related to DOM interactions and controls
  events: async () => {}
};

export default RedFlag;
